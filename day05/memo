반복문 - for 






*단어들 외우기
factorial(팩토리알)
result


*배열(객체)

var seasons = [ "spring", "summer", "fall", "winter"]
undefined
seasons
(4) ['spring', 'summer', 'fall', 'winter']
0: "spring"
1: "summer"
2: "fall"
3: "winter"
length: 4
[[Prototype]]: Array(0)


배열은?
여러 개의 연속적인 값 저장하고자 할 때 사용하는 자료형

배열 사용 장점
인덱스 이용하여 순차적 관리 -> 효율적

var 배열 이름 = [값1, 값2, 값3...]    문자형 배열  (이걸 더 많이 쓰는듯)
var 배열 이름 = new Array(5)          숫자형 배열


length에서 .을 항목 연산자 라고 한다

교재 557p~559p 읽어보기 (객체)
Array의 객체 메서드(함수)
push() - 추가
pop() - 빼기, 삭제
concat() - 연결
indexOf() - 위치(숫자)

() 여기 안에 들어가는걸 매개변수?라고 한다.

splice 혼자서 추가도 하고 삭제도 가능하다
concat
join


1 3 5 7 9 25
_________ __
sum       push(맨뒤에 추가)     이용하기



*for in 반복문

for(var 변수 in 배열이름){

}



*진수 표현
-
-
-

아스키코드

유니코드(1640개??)가 아스키코드(128개)를 포함시켰다
인코딩 포맷
UTF-8 UTF-16

String.fromCharCode
__.charCodeAt




*함수 (function)
특정한 하나의 기능을 수행하는 일련의 코드

-명시적 함수  (이걸 더 많이 쓴다)
-익명 함수


function은 프로그램이 읽다가 호출하면 찾아간다



*지역변수의 유효범위

-지역 변수
함수랑 연동할때는 변수이용할때 신경써야한다!!
블럭(지역) 안에 x가 정의되어 있을때는 지역 안에서만 x가 정의된 것
그 밖에서는 정의가 해제되어 x를 입력해도 x가 뭔지 컴퓨터가 모름
